#ifndef ISIDORE_STRING_UTILS
#define ISIDORE_STRING_UTILS
#include <string>
std::string ParseOutFileName(std::string input);
std::string ParseOutExtension(std::string input);
std::string SimplifyPath(std::string input);
#endif
