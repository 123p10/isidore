extern putchard(ch);
void binary : 1 (x y) y;
void line(n)
    for i = 1, i < n, 1.0 in
        putchard(42);
void box(x)
    for i = 1, i < x, 1.0 in
        line(x) : putchard(10);
box(5);
These are out of date btw