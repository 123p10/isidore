extern printd(x);

void loop(x){
    if(x < 10){
        printd(10);
        return 1;
    }
    else{
        printd(12);
    };
    printd(1);
    return 0;
}
loop(11);
//Really didn't think it would work but it does