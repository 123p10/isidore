Syntax Definition
    -Establish return keyword/statements
    -Redefine for statements to utilize {}(rewrite for statements)
    -redefine functions to utilize {}
    -Proper type definitions for functions
Develop proper source build system
Develop proper compilation system
Compile to object files
Debugger Compatibility
Proper type systems
String type
Char type
Remove automatic type inferencing
Define while loop
Define arrays
Proper Error Handling!(Define is Syntax error or pointer error etc, line numbers, Expected x got y)
Reduce runtime by converting getCurrToken() to proper storage of currentToken
Multi-char operators, ==, ===, <=, >=
Improve Kaleidiscope JIT for better optimizations
Change and establish proper handling of semicolons, should prob not be handled directly in if
Make return statements terminate, not be changeable(this causes some major issues so gonna have to fix this soon)


ALWAYS REMOVE WARNINGS